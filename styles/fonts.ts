import { Montserrat } from "next/font/google";
import { Open_Sans } from "next/font/google";

export const header = Montserrat({ subsets: ['latin'] });
export const body = Open_Sans({ subsets: ['latin'] });

